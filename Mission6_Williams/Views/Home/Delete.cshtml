@model Mission6_Williams.Models.Movie

@{
    ViewData["Title"] = "Delete Movie";
}

<div class="container mt-5">
    <h2 class="text-danger">Confirm Deletion</h2>
    <div class="alert alert-warning">
        Are you sure you want to delete the movie <strong>@Model.Title</strong>?
    </div>

    <form method="post" asp-action="Delete">
        <input type="hidden" asp-for="MovieId" />
        
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-controller="Home" asp-action="MovieList" class="btn btn-secondary">Cancel</a>
    </form>
</div>